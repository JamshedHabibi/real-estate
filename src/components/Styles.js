import styled from 'styled-components'

export const SearchButton = styled.span`
    background: rgb(135, 89, 89);
    border-color: rgb(135, 89, 89);
    color: white;
    height: 40px;
    position: relative;
    top: -3.6vh;
    width: 100px;

.searchBtn-text {
    padding: 10px 15px 6px 15px;
    font-size: 0.9em;
    letter-spacing: 0.08em;
}
`